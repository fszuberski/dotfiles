- name: Install required packages from the AUR 
  hosts: localhost

  tasks:
    - name: Install required packages from the AUR
      yay:
        name:
          - google-chrome
          - gotop
          - nvim-packer-git	
          - nvm
          - otf-nerd-fonts-fira-code
          - picom-ibhagwan-git
          - spotify
          - ttf-meslo-nerd-font-powerlevel10k
          - uxplay-git
      become: yes
      become_user: root
